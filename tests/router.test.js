const router = require('../app/router');

describe('API response', () => {

// The get request should return a status 200
// The get request should return a json
// The json returned from get request should contain a string

});
